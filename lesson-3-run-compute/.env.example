RPC_URL="https://evmrpc-testnet.0g.ai"
INDEXER_RPC="https://indexer-storage-testnet-turbo.0g.ai"
PRIVATE_KEY="0x..."           # needs testnet funds

# Optional: if you have a real compute endpoint, set this:
# COMPUTE_URL="https://your-compute-service/summarize"

# Optional: where to read input CID from (defaults to lesson-2 artifact)
# INPUT_CID_ARTIFACT="../lesson-2-upload-storage/artifacts/inputCid.json"
